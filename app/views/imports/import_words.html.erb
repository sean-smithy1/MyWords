<h1>Import words into <%= @word_import.list_name %></h1>

<p>A CSV, Excel or Open Document Speadsheet can be used to import words into your list. You are allowed to import up to <%= List.maxwords %> words per list. You have <%= @list.words_remaining %> remaining in this list. Words beyond this limit will be not be imported</p>

<ul>
       <li>
        <strong>Word Format</strong> -
        upto  characters with no spaces.
      </li>
      <li>
        <strong>Duplicates</strong> - There should be no duplicate words in your list.
      </li>
      <li>
        <strong>Capitalisation will be maintained</strong> - There is no easy way for us to determine capitalisation, you will need to do this.
      </li>
</ul>

<%= form_for @word_import do |f| %>
  <% if @word_import.errors.any? %>

    <div id="error_explanation">
      <h2><%= pluralize(@word_import.errors.count, "error") %> prohibited this import from completing:</h2>
      <ul>
        <% @word_import.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>

  <% end %>

  <div class="field">
    <%= f.file_field :file %>
  </div>

  <div class="field">
    <%= f.hidden_field :list_id %>
  </div>

  <div class="buttons">
    <%= f.submit "Import" %>
  </div>

<% end %>
